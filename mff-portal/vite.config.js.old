// vite.config.js
import { defineConfig } from 'vite'

export default defineConfig({
  server: {
    host: '0.0.0.0',            // já deixava desta forma
    port: 4200,                 // mesma porta que expõe no Docker
    strictPort: true,           // falha se a porta já estiver em uso
    allowedHosts: 'all'
  }
})
